<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <stroke
        android:width="1dp"
        android:color="@color/redColorDark" />
    <corners android:radius="50dp" />
    <gradient
        android:angle="90"
        android:centerColor="@color/redColorDark"
        android:endColor="@color/redColorDark"
        android:startColor="@color/redColorDark" />
</shape>