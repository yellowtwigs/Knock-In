<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- This is the stroke you want to define -->
    <stroke android:width="0.10dp"
        android:color="?attr/borderViewColor"/>
    <gradient android:startColor="?attr/relativeLayoutPressedColor"
        android:centerColor="?attr/relativeLayoutPressedColor"
        android:endColor="?attr/relativeLayoutPressedColor"
        android:angle="90"/>
</shape>