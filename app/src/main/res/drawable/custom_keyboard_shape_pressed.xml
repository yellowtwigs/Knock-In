<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- This is the stroke you want to define -->
    <stroke android:width="0.30dp"
        android:color="@color/whiteGreyColorDark"/>
    <gradient android:startColor="@color/whiteGreyColor"
        android:centerColor="@color/whiteGreyColor"
        android:endColor="@color/whiteGreyColor"
        android:angle="90"/>
</shape>