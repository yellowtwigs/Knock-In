<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- This is the stroke you want to define -->
    <stroke android:width="0.30dp"
        android:color="@color/borderViewColorDark"/>
    <gradient android:startColor="@color/borderViewColor"
        android:centerColor="@color/borderViewColor"
        android:endColor="@color/borderViewColor"
        android:angle="90"/>
</shape>