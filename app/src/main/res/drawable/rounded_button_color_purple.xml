<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <stroke
        android:width="1dp"
        android:color="@color/purple_tag_group" />
    <corners android:radius="50dp" />
    <gradient
        android:angle="90"
        android:centerColor="@color/purple_tag_group"
        android:endColor="@color/purple_tag_group"
        android:startColor="@color/purple_tag_group" />
</shape>