<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- This is the stroke you want to define -->
    <stroke android:width="0.10dp"
        android:color="@color/lightGreyColor"/>
    <gradient android:startColor="@color/backgroundPressedColor"
        android:centerColor="@color/backgroundPressedColor"
        android:endColor="@color/backgroundPressedColor"
        android:angle="90"/>
</shape>