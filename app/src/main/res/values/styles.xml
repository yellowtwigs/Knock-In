<resources>

    <!-- Base application theme. -->
    <!-- Light/Day Mode theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="colorPrimary">@color/knockerColorPrimary</item>
        <item name="colorPrimaryDark">@color/knockerColorPrimaryDark</item>
        <item name="backgroundColor">@color/backgroundColor</item>
        <item name="textColor">@color/textColor</item>
        <item name="textColorLight">@color/textColorLight</item>
        <item name="relativeLayoutPressedColor">@color/relativeLayoutPressedColor</item>
        <item name="borderViewColor">@color/borderViewColor</item>
        <item name="colorSurface">@color/borderViewColor</item>
        <item name="android:windowAnimationStyle">@style/noAnimTheme</item>
    </style>

    <style name="windowAnimationTransition">
        <item name="android:windowEnterAnimation">@android:anim/fade_in</item>
        <item name="android:windowExitAnimation">@android:anim/fade_out</item>
    </style>


    <style name="noAnimTheme" parent="android:Theme">
        <item name="android:windowAnimationStyle">@null</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
