<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/sort"
        android:icon="@drawable/ic_filter"
        android:title="@string/filter_by"
        app:showAsAction="ifRoom">
        <menu>
            <group android:checkableBehavior="single">
                <item
                    android:id="@+id/item_daily"
                    android:checked="true"
                    android:icon="@drawable/ic_daily"
                    android:orderInCategory="100"
                    android:title="@string/dashboard_daily"
                    app:showAsAction="collapseActionView" />
                <item
                    android:id="@+id/item_weekly"
                    android:icon="@drawable/ic_weekly"
                    android:orderInCategory="100"
                    android:title="@string/dashboard_weekly"
                    app:showAsAction="collapseActionView" />
                <item
                    android:id="@+id/item_monthly"
                    android:icon="@drawable/ic_monthly"
                    android:orderInCategory="100"
                    android:title="@string/dashboard_monthly"
                    app:showAsAction="collapseActionView" />
            </group>
        </menu>
    </item>

    <group
        android:checkableBehavior="single"
        android:menuCategory="container">

    </group>
</menu>